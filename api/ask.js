// The Vercel function. Everything is in src/ai/ask-handler.js so the dev server
// runs the same code — see the askEndpoint plugin in vite.config.js.
export { default } from '../src/ai/ask-handler.js';
